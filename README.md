# Altea - Addiction Recovery App

> Приложение для отслеживания восстановления от зависимостей

## 🎯 Возможности

### Основные функции
- ✅ **Кастомная модель пользователя** - Аутентификация по email
- ✅ **Система аутентификации** - Вход, Регистрация, Сброс пароля
- ✅ **Material Design 3 UI** - Современный интерфейс
- ✅ **Email шаблоны** - Расширяемая система шаблонов
- ✅ **Dashboard** - Боковая панель, навбар, меню пользователя
- ✅ **Docker Setup** - PostgreSQL, Redis, pgAdmin

### Технологии
- **Backend:** Django 5.0+, Python 3.12
- **Database:** PostgreSQL 15.8
- **Cache:** Redis 7.2
- **Frontend:** Bootstrap 5.3, Material Design 3
- **Container:** Docker & Docker Compose

## 🚀 Quick Start

### Быстрый запуск (одна команда)

Убедитесь, что Docker Desktop запущен, затем выполните:

```bash
./start.sh

# Или если не работает:
bash start.sh
```

Скрипт автоматически:
- Создаст `.env` файл из примера
- Запустит Docker контейнеры (PostgreSQL, Redis)
- Создаст виртуальное окружение Python
- Установит все зависимости
- Применит миграции базы данных
- Запустит сервер разработки

После запуска:
- **Приложение:** http://127.0.0.1:8000
- **Админка:** http://127.0.0.1:8000/admin

**Суперадмин для локальной разработки:**
- Email: `admin@altea.com`
- Пароль: `password123`

Для создания нового суперпользователя (в новом терминале):
```bash
source venv/bin/activate
python manage.py createsuperuser
```

---

### Ручной запуск (пошагово)

#### 1. Настройка окружения
```bash
cp .env.example .env
```

#### 2. Запуск Docker сервисов
```bash
docker compose up -d
```

#### 3. Настройка Django
```bash
# Создать виртуальное окружение
python3 -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate

# Установить зависимости
pip install -r requirements.txt

# Применить миграции
python manage.py migrate

# Создать суперпользователя
python manage.py createsuperuser

# Запустить сервер
python manage.py runserver
```

#### 4. Открыть приложение

- **App:** http://localhost:8000
- **Admin:** http://localhost:8000/admin
- **pgAdmin:** http://localhost:15433

## 🐳 Docker команды

```bash
# Запустить сервисы
docker compose up -d

# Остановить сервисы
docker compose down

# Посмотреть логи
docker compose logs -f

# Перезапустить сервисы
docker compose restart

# Запустить с pgAdmin
docker compose --profile tools up -d
```

## 🧪 Тестирование

```bash
# Запустить тесты
pytest

# С покрытием
pytest --cov=apps --cov-report=html

# Конкретное приложение
pytest apps/accounts/tests/
```

## 📦 Структура проекта

```
altea/
├── apps/                    # Django приложения
│   ├── accounts/           # Аутентификация
│   ├── dashboard/          # Главная панель
│   └── core/               # Общие утилиты
├── config/                  # Настройки Django
│   ├── settings/
│   │   ├── base.py
│   │   ├── development.py
│   │   └── production.py
│   ├── urls.py
│   ├── wsgi.py
│   └── asgi.py
├── templates/               # Глобальные шаблоны
│   ├── base.html
│   ├── layouts/
│   ├── includes/
│   └── emails/
├── static/                  # Статические файлы
├── docker-compose.yml       # Docker конфигурация
├── requirements.txt         # Python зависимости
└── start.sh                 # Скрипт быстрого запуска
```

## 🔐 Безопасность (перед продакшеном)

- [ ] Изменить `SECRET_KEY` в `.env`
- [ ] Установить `DEBUG=False`
- [ ] Настроить `ALLOWED_HOSTS`
- [ ] Настроить SSL/HTTPS
- [ ] Настроить production email backend
- [ ] Настроить резервное копирование PostgreSQL
